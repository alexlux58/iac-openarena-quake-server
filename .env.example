# OpenArena AWS - Environment Variables
# Copy this file to .env and fill in your values
# .env is gitignored and should NEVER be committed

# ============================================================================
# AWS CONFIGURATION
# ============================================================================

AWS_REGION="us-west-2"
AWS_PROFILE=""  # Optional: AWS CLI profile name (leave empty to use default)

# ============================================================================
# EC2 INSTANCE CONFIGURATION
# ============================================================================

INSTANCE_TYPE="t2.micro"  # Free tier eligible

# ============================================================================
# SSH CONFIGURATION
# ============================================================================

SSH_KEY_NAME="my-key-pair"  # Your AWS SSH key pair name
CREATE_KEY_PAIR="false"     # Set to "true" to create a new key pair
SSH_PUBLIC_KEY_FILE="~/.ssh/id_rsa.pub"  # Path to your SSH public key
SSH_PRIVATE_KEY_FILE="~/.ssh/id_rsa"      # Path to your SSH private key
SSH_ALLOWED_CIDR="203.0.113.10/32"       # Your public IP for security (use /32 for single IP)

# ============================================================================
# CLOUDFLARE CONFIGURATION (Optional - for custom DNS)
# ============================================================================
# IMPORTANT: Even if NOT using Cloudflare, set CLOUDFLARE_API_TOKEN to avoid errors
# Set to "dummy" if not using Cloudflare (provider needs a value but won't be used)

CLOUDFLARE_API_TOKEN=""  # Set your token here if using Cloudflare, or "dummy" if not
CLOUDFLARE_ZONE_ID=""    # Leave empty if not using Cloudflare DNS
CLOUDFLARE_ZONE_NAME=""  # Leave empty if not using Cloudflare DNS
CLOUDFLARE_SUBDOMAIN="quake"  # Only used if zone_id/zone_name are set
CLOUDFLARE_TTL="300"

# ============================================================================
# NOTES
# ============================================================================
# 1. For Cloudflare, it's recommended to use the CLOUDFLARE_API_TOKEN 
#    environment variable instead of setting it in terraform.tfvars for better security.
#
# 2. For AWS credentials, the scripts will use:
#    - Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
#    - AWS CLI profile (if AWS_PROFILE is set)
#    - ~/.aws/credentials and ~/.aws/config files
#
# 3. SSH_ALLOWED_CIDR should be your public IP address with /32 suffix
#    Find your IP: curl ifconfig.me
#    Example: SSH_ALLOWED_CIDR="203.0.113.10/32"
#
# 4. The layered-deploy script uses terraform.tfvars for Terraform variables.
#    This .env file is mainly for deploy.sh and SSH configuration.
# ============================================================================
